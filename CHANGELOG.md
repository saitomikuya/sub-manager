# Changelog

## [1.0.1] - 2026-08-18

### Fixed

- 新增经过启动校验的 `TRUSTED_PROXIES` 环境变量，支持逗号分隔的 IPv4、IPv6、单 IP 和 CIDR。
- 仅在 TCP 直接连接方受信任时读取代理头，并从右向左安全解析 `X-Forwarded-For` 代理链。
- 将访问记录、最后拉取 IP、登录限流和请求日志统一到同一个客户端 IP 解析结果。
- 对畸形代理头、多个 `X-Real-IP` 值和 IPv4-mapped IPv6 地址增加安全处理与测试。
- 可信代理 IP/CIDR 可在后台“系统设置”中校验、保存并即时生效，同时兼容环境变量配置。
- 未配置可信代理范围时默认接受所有直接来源的代理头，免去 Caddy 常规部署的手动配置；后台或环境变量仍可收紧范围。

### Documentation

- 增加 Caddy 通过 Docker 网关反代的配置示例。
- 增加 Caddy 与 SubManager 共用自定义 Docker 网络、且不公开应用端口的推荐部署方式。

## [1.0.0] - 2026-08-17

- 首个公开版本。
